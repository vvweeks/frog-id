# Copy to .env (which is gitignored) and fill in real values.
# Locally, load with: from dotenv import load_dotenv; load_dotenv()
XC_API_KEY=your_xeno_canto_api_key_here
INAT_CONTACT_EMAIL=your_email@example.com
FROG_ID_SAVE_DIR=./6575_Deep_Learning
